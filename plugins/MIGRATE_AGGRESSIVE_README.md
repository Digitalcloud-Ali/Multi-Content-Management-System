# Aggressive migration (completed)

The legacy root modules have been moved into `plugins/<module>/www/` on branch `feature/finish-module-migration`.

Prefer the documented flow in `plugins/README.md`.

This script is kept only as a historical helper. Do not re-run it on an already-migrated tree.
